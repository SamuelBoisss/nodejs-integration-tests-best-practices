name: Node.js CI

on:
  push:
    branches:
      - '*'
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Read .nvmrc
        run: echo "##[set-output name=NVMRC;]$(cat .nvmrc)"
        id: nvm

      - name: Use Node.js (.nvmrc)
        uses: actions/setup-node@v2
        with:
          node-version: "${{ steps.nvm.outputs.NVMRC }}"

      # Cache node_modules
      - name: Cache node_modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json', '**/.nvmrc') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      # Installer Docker Compose
      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - run: npm ci  # Installer toutes les dépendances

  test:
    runs-on: ubuntu-latest
    needs: build  # Ce job dépend du job 'build'
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js (.nvmrc)
        uses: actions/setup-node@v2
        with:
          node-version: "${{ steps.nvm.outputs.NVMRC }}"
      - run: npm ci  # Installer les dépendances avant d'exécuter les tests
      - run: npm test  # Exécuter les tests principaux

  test-nestjs:
    runs-on: ubuntu-latest
    needs: build  # Ce job dépend du job 'build'
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js (.nvmrc)
        uses: actions/setup-node@v2
        with:
          node-version: "${{ steps.nvm.outputs.NVMRC }}"
      - run: npm ci  # Installer les dépendances avant d'exécuter les tests
      - run: npm run test:nestjs  # Exécuter les tests NestJS

  test-mocha:
    runs-on: ubuntu-latest
    needs: build  # Ce job dépend du job 'build'
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js (.nvmrc)
        uses: actions/setup-node@v2
        with:
          node-version: "${{ steps.nvm.outputs.NVMRC }}"
      - run: npm ci  # Installer les dépendances avant d'exécuter les tests
      - run: npm run test:mocha  # Exécuter les tests Mocha
